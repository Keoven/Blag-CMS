<h1>List of All Articles</h1>
<div class="toppadding">
  <% if @articles.nitems != 0 %>
  <table border="0" cellpadding="0" cellspacing="0">
    <tr id="head">
      <th>Title</th>
      <th>Body</th>
      <th>Status</th>
      <th>Author</th>
      <th colspan="4">Date Posted</th>
    </tr>
    <% i = 0 %>
    <% @articles.each do |article|%>
    <tr class="<%= i % 2 == 1 ? "rowodd" : "roweven" %>">
      <td><%= truncate(article.title, :length => 10) %></td>
      <td><%= truncate(article.body, :length => 10) %></td>
      <td><%= article.status %></td>
      <td><%= full_name(Author.find(article.author_id)) %></td>
      <td><%= formatted_date_posted(article) %></td>
      <td class="article-index-entry"><%= link_to "Edit", edit_article_path(article), :class => "offlink" %></td>
      <td class="article-index-entry"><%= link_to "Show", article_path(article), :class => "offlink" %></td>
      <td class="article-index-entry"><%= link_to "Delete", article, :confirm => "Are you sure you wane to delete article \"#{article.title}\"?", :method => :delete, :class => "offlink" %></td>
    </tr>
    <% i += 1 %>
    <% end %>
  </table>
  <% end %>
<hr/>
<%= link_to "Create New Article", new_article_path, :style => "float: right; margin-right: 10px;" %>
</div>
